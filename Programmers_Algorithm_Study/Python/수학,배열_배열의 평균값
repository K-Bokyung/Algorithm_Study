# https://school.programmers.co.kr/learn/courses/30/lessons/120817

def solution(numbers):
    answer = 0
    plus = 0
    for i in range(len(numbers)):
        plus += numbers[i]
        answer = plus/len(numbers)
    return answer
